# 2026-03-29 日记（周日）

## 今日主要事件

### 1. 每日热点公众号创作 ✅（07:00 cron）
- **方向**：AI Agent 实用化
- **标题**：《AI Agent实用化一年：哪些真香，哪些画饼？》
- **状态**：成功推送草稿箱，封面图已生成
- **脚本**：`/home/hochoy/.openclaw/workspace-wechat-assistant/skills/daily-hot-content/script.py`
- **env 问题**：WECHAT_APP_ID/WECHAT_APP_SECRET vs WECHAT_APPID/WECHANT_SECRET，已用 export 临时解决

### 2. OpenClaw Release Watcher ✅（10:00 cron）
- **版本**：v2026.3.28-beta.1（重大更新）
- **Breaking Changes**：
  - Qwen Provider 移除旧版 OAuth，需迁移至 Model Studio
  - Config/Doctor 取消两个月以上的自动迁移
- **重点新特性**：
  - xAI → Responses API + x_search（配置更简单）
  - MiniMax image-01 生图正式支持（图生图 + 长宽比控制）
  - 插件 before_tool_call 钩子支持 requireApproval（可暂停等待确认）
  - ACP binds 扩展至 Discord/iMessage（当前对话直接绑定无需子线程）
  - Slack 显式文件上传 + 跨平台统一 upload-file
  - Matrix TTS 改为原生语音气泡
  - MiniMax 模型精简至仅 M2.7
- **知识库**：`/home/hochoy/.openclaw/workspace-coder/docs/openclaw-docs.md` 已同步更新

### 3. DeerFlow × OpenClaw 深度集成讨论（22:37）
- **触发**：用户向 coder 询问 DeerFlow 接入 OpenClaw 的可行性
- **coder 给出 5 种方案**：
  1. OpenClaw Feishu ↔ DeerFlow Feishu 并存（⭐ 简单）
  2. **OpenClaw 前端 + DeerFlow 执行层**（⭐⭐⭐ **推荐，立即可用**）
  3. DeerFlow 作为 ACP Agent 接入（⭐⭐⭐ 深度，需 ACP Bridge）
  4. DeerFlow 作为 MCP Server（⭐ 浅层工具借用）
  5. OpenClaw + DeerFlow 双 Bot 协作（飞书特定）
- **核心结论**：
  - 两者协议语义高度重合，ACP Bridge 是长期最优解
  - 方案 2 只需写一个 OpenClaw Skill 调用 DeerFlowClient，1~2天可完成原型
- **待确认**：用户倾向哪个方向

### 4. 主会话：多 agent 通信讨论（何才）
- **主题**：与 coder 交互式通信的实现方案
- **触发**：`跟coder 交互式触发的`
- **讨论**：通过 `sessions_send` 工具向 coder 发消息并获取回复
- **配置**：需开启 `tools.agentToAgent`，配置 `allow: ["main", "coder"]`
- **方案 A**（推荐）：开启 `tools.agentToAgent`
- **方案 B**：通过飞书 Group 桥接
- **方案 C**：ACP Sessions（Claude Code 集成用）
- **待确认**：用户希望单向（main → coder）还是双向通信

### 5. 群聊 @all 测试
- coder、demo、wechat-assistant、task-manager 四个 agent 飞书群均正常响应 @all "are you ok"

## 今日 cron 记录
| 时间 | 任务 | 状态 |
|------|------|------|
| 07:00 | 每日热点公众号创作 | ✅ |
| 10:00 | OpenClaw Release Watcher（task-manager） | ✅ |
| 23:00 | 每日23点记忆总结 | ✅ |

## 经验沉淀
- 公众号草稿 API：必须用 `material/add_material`（永久素材）
- isolated session 发飞书消息：必须加 `accountId="main"`
- 内容创作类 cron：timeout 建议 ≥1800 秒

## 待关注
- DeerFlow 集成方向（方案 2 还是方案 3）待用户确认
- OpenClaw v2026.3.28-beta.1 新特性评估
- 多 agent 通信（session_send ↔ coder）是否需要开启 `tools.agentToAgent`
