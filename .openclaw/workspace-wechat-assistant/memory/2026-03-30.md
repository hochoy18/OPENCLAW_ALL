# 每日记忆 | 2026-03-30（周一）

## 今日主要事件

### 1. OpenClaw 升级完成 ✅
- 升级到 2026.3.28（f9b1079）
- 用户确认后执行，重启完成

### 2. 文档更新检查 ✅
- 检测到 xAI Responses API 升级、MiniMax image-01 生图上线、安全插件钩子改进
- 知识库 `openclaw-docs.md` 已同步更新（版本改为 2026.3.28，新增 Bug Fixes 部分）

### 3. 每日热点公众号创作 ✅
- 选题：AI Agent 爆发
- 标题：《2026年AI Agent彻底炸了，80%的人还在看热闹》
- 封面图：MiniMax image-01，16:9 科技感蓝色调
- 草稿箱：发布成功，media_id=VI03CdGujbFMX0KhlRuwQ5-UBmsUU_R6rB2TKBTLmJ0eTBEVoCJwjHRCw3tcVLAN
- ⚠️ 飞书通知发送失败（isolated cron session 400 错误，无法路由到用户）

### 4. 群聊多 Agent 身份汇报 ✅
- coder、demo、wechat-assistant、task-manager、main 5个 agent 同时响应了群聊 @_all 查询
- 各自介绍了 workspace、模型、版本、skills 列表

### 5. 飞书群组配置咨询 ✅
- 解释了 `groupPolicy` + `requireMention` 的关系
- 当前配置可正常接收群消息，只在 @ 时回复
- 建议：只改 `requireMention`，不动 `groupPolicy`

### 6. Cron 任务错误排查 ✅
- 3个 error 状态 cron 任务（demo、cron、wechat-mp-writer）
- 原因：飞书投递缺少 target 配置
- 建议：删除这3个重复/配置错误的 cron

### 7. VoicePix V2 语音生成 Bug 修复 ✅
- 问题：连续生成多次语音时旧 `audioSource` 冲突导致状态混乱
- 修复：`pauseAudio`/`rewindAudio`/`forwardAudio` 逻辑统一用 `audioStartTime`；生成前先调用 `stopAudio()` 清理旧状态
- GitHub 仓库：https://github.com/hochoy18/voicepix_v2
- 项目运行中：http://localhost:3000
- ⚠️ 用户 20:53 报告 JS 报错（`generateSpeech is not defined` 等），待下次排查

### 8. 飞书插件 Warning 问题 ✅
- 用户遇到飞书插件冲突 warning
- 用户已自行重启解决

## 待关注
- VoicePix V2 的 JS 报错需排查（`generateSpeech is not defined`）
- 3个 error cron 任务待确认是否删除
- 每日热点公众号飞书通知在 isolated session 中失败问题

## 经验沉淀
- isolated cron session + Feishu 消息发送 → 需要 `accountId="main"` 指定主账号
- 飞书 `Delivering to Feishu requires target` → cron job 缺少飞书投递 target 配置
