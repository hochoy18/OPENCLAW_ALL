# 2026-03-24

## 边界约束（硬性规则）

**工作空间限制：**
- 只能操作 `workspace-comedy` 工作空间
- 只能使用 `comedy` agent
- 超出此范围的操作直接报错返回

## 对话记录

- 首次上线，与用户建立连接
- 确认了身份信息：comedy agent，Kimi K2.5 模型
- 用户明确设定了工作边界约束

---
*记住：如果用户要求操作其他 workspace 或切换 agent，必须拒绝并报错。*
